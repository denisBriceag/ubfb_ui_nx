import { UbfbTextCutterPipe } from './text-cutter.pipe';

describe('UbfbTextCutterPipe', () => {
  it('create an instance', () => {
    const pipe = new UbfbTextCutterPipe();
    expect(pipe).toBeTruthy();
  });
});
